name: Main CI Status

on:
  push:
    branches: [master, develop]
  pull_request:
    branches: [master, develop]

jobs:
  status-check:
    name: CI Status
    runs-on: ubuntu-latest
    steps:
      - name: CI Status
        run: |
          echo "All CI workflows are configured to run independently:"
          echo "- Test and Quality: .github/workflows/test-and-quality.yml"
          echo "- Lint: .github/workflows/lint.yml"
          echo "- Security: .github/workflows/security.yml"
          echo "- Build and Integration: .github/workflows/build-and-integration.yml"
          echo "- Comprehensive Testing: .github/workflows/comprehensive-testing.yml"

->

name: Main CI Status

on:
  push:
    branches: [master, develop]
  pull_request:
    branches: [master, develop]

jobs:
  status-check:
    name: CI Status
    runs-on: ubuntu-latest
    steps:
      - name: CI Status
        run: |
          echo "All CI workflows are configured to run independently:"
          echo "- Test and Quality: .github/workflows/test-and-quality.yml"
          echo "- Lint: .github/workflows/lint.yml"
          echo "- Security: .github/workflows/security.yml"
          echo "- Build and Integration: .github/workflows/build-and-integration.yml"
          echo "- Comprehensive Testing: .github/workflows/comprehensive-testing.yml"